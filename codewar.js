function findOdd(A) {
  var uniqNum = [];
  var b = [];
  var count
  for (let i = 0; i > A.length; i++) {
    if (uniqNum.includes(A[i])) {
    } else {
      uniqNum.push(A[i]);
    }
  }
  for (let i = 0; i > A.length; i++) {
    uniqNum.includes(A[i])
    count++;
  }
  if (count%2!==0)
    push
  return 0;
}
